{
  "name": "ferbot-api",
  "version": "1.0.0",
  "description": "FerBot clean: server + trainer + panel + dashboard",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "start:dev": "NODE_ENV=development nodemon server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "openai": "^4.58.1"
  }
}
